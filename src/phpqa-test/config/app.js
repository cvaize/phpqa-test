const COMMAND = {
    RUN: 'run',
    CHUNK: 'chunk',
    JOIN: 'join',
}

const PHPQA = {
    TOOLS: {
        PHPMETRICS: 'phpmetrics',
        PHPMD: 'phpmd',
        PDEPEND: 'pdepend',
        PHPCS: 'phpcs',
        PHPCPD: 'phpcpd',
        PHPLOC: 'phploc',
    }
}

const vars = {
    COMMAND,
    PHPQA,
}

module.exports = vars
