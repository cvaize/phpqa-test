const PHPQA = {
    TOOLS: {
        PHPMETRICS: 'phpmetrics',
        PHPMD: 'phpmd',
        PDEPEND: 'pdepend',
        PHPCS: 'phpcs',
        PHPCPD: 'phpcpd',
        PHPLOC: 'phploc',
    }
}

const vars = {
    PHPQA,
}

module.exports = vars
